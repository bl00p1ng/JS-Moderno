// Crear Array
const numbers = [10, 20, 30, 40];

// Otra forma de crear arreglos
const months = new Array('Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo');

console.log(numbers);
console.log(months);

// Un array que contiene datos de todo tipo
const everything = ['Hola', 10, true, {name: 'Andrés', work: 'Developer'}, null, [1, 2, 3]];
console.log(everything);