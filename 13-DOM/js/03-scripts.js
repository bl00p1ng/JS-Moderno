// Acceder a un elemento por su ID
const form = document.getElementById('formulario');
console.log(form);

// Si el ID no existe
const dontExist = document.getElementById('dont-exist');
console.log(dontExist);