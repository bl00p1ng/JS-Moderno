const isAuthenticated = true;

if (isAuthenticated) {
    console.log('Si puedes ver Netflix');
} else {
    console.log('No puedes ver Netflix :(');
}

// Operador ternario
console.log(isAuthenticated ? 'Si esta autenticado' : 'No esta autenticado');