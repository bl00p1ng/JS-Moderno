const name = 'Monitor 20 pulgadas';
const price = 350;
const isAvailable = true;

// Un objeto puede agrupar todo lo anterior en una sola variable

// Object Literal
const product = {
    name: 'Monitor 20 pulgadas',
    price: 350,
    isAvailable: true
};

console.table(product);